# use lego for ssl
```sh
export GOOGLE_DOMAINS_ACCESS_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
lego --email you@example.com --dns googledomains --domains 'first.example.org' --domains 'second.example.org' run
```
